var assert = require('assert');

describe('TrackSourceService allows you to list, create, and delete track sources', function(){
  it('should be able to create a tracksource of a known type', function(done) {
    assert(false);
  });

  it('should be able to list all known tracksources', function(done){
    assert(false);
  });

  it('should allow you to delete a tracksource', function(done){
    assert(false);
  });

  it('should not allow you to delete something thats not there', function(done){
    assert(false);
  });
});